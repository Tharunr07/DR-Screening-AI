# Phase 1.5 — Real Dataset Acquisition & Verification Report

> **Phase 1 locked**: `validatePhase1 = PASS 10/10` (empty + synthetic 23-row evidence)  
> **Phase 1.5 scope**: ONLY prepare/inspect/acquire/register/verify real datasets. No Phase 2.  
> **Date**: 2026-08-30  
> **Working directory**: `C:\dev\SIH\DR_Screening` (`datasetConfig.m:15`)  
> **Gate**: `APTOS>0 AND IDRiD>0 AND DRIVE>0 AND Messidor2>0 AND validatePhase1 PASS` → `REAL DATASET GATE PASSED` else `DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED`

---

## A. Dataset Acquisition Status

| Dataset | Present | Images | Labels | Annotations | Role | Expected destination (absolute) | Status |
|---------|---------|:------:|--------|-------------|------|---------------------------------|--------|
| **APTOS 2019** | **NO** | 0 | 0 | 0 | DR severity classification (0–4) | `C:\dev\SIH\DR_Screening\data\raw\APTOS2019\` | **DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED** |
| **IDRiD** | **NO** | 0 | 0 | 0 | DR grading + lesion research (MA/HE/EX/SE/OD) | `C:\dev\SIH\DR_Screening\data\raw\IDRiD\` | **DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED** |
| **Messidor-2** | **NO** | 0 | 0 | 0 | External validation (isolated) | `C:\dev\SIH\DR_Screening\data\raw\Messidor2\` | **DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED** |
| **DRIVE** | **NO** | 0 | 0 | 0 | Vessel segmentation (ground-truth masks) | `C:\dev\SIH\DR_Screening\data\raw\DRIVE\` | **DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED** |

No datasets were automatically downloaded — acquisition was **not** attempted from unofficial mirrors per policy `docs/DATASET_PROVENANCE.md:96` and `docs/DATASET_DOWNLOAD_GUIDE.md:3`. All four official sources require authentication/approval:

- APTOS: Kaggle `Join Competition` → https://www.kaggle.com/c/aptos2019-blindness-detection
- IDRiD: IEEE DataPort sign-in → https://ieeedataport.org/open-access/indian-diabetic-retinopathy-image-dataset-idrid
- DRIVE: Grand Challenge registration → https://drive.grand-challenge.org/
- Messidor-2: ADCIS form → https://www.adcis.net/en/third-party/messidor2/

**Result**: gate fails — all four missing. Correctly reported, not fabricated. Synthetic evidence in `results/synthetic_test_evidence/` (23 rows) proves loader handles real images when present, but production `data/raw/` remains empty.

---

## B. Actual Filesystem Structure (Verified 2026-08-30)

### Expected (per `docs/DATASET_DOWNLOAD_GUIDE.md:24` and `docs/DATASET_PROVENANCE.md:33`)

```
data/raw/APTOS2019/
  train_images/*.png  (3662)
  test_images/*.png   (1928)
  train.csv           (id_code, diagnosis 0–4)

data/raw/IDRiD/
  A. Segmentation/
    1. Original Images/          *.jpg
    2. All Segmentation Groundtruths/
       Microaneurysms/  *_MA.tif
       Haemorrhages/    *_HE.tif
       Hard Exudates/   *_EX.tif
       Soft Exudates/   *_SE.tif
       Optic Disc/      *_OD.tif
  B. Disease Grading/
    1. Original Images/          *.jpg
    2. Groundtruths/             *.csv

data/raw/DRIVE/
  training/images/     *.tif (20)
  training/1st_manual/ *.gif (20)
  training/mask/       *.gif (20)
  test/images/         *.tif (20)
  test/1st_manual/     *.gif
  test/mask/           *.gif

data/raw/Messidor2/
  *.tif/*.jpg (1748 images) + optional grades.csv/xlsx
```

### Actual (honest, `dir` inspected)

```
data/raw/APTOS2019/   → .gitkeep (0 bytes) only
data/raw/IDRiD/       → .gitkeep only
data/raw/DRIVE/       → .gitkeep only
data/raw/Messidor2/   → .gitkeep only
data/processed/       → manifest.csv (290 B header-only) + manifest.mat (1154 B)
data/splits/          → train.csv 290 B, val.csv 290 B, test.csv 290 B, external.csv 290 B, split_metadata.json 668 B (all header-only, 0 rows)
results/              → audit_results.json 3105 B, phase1_validation.json 1288 B, *_registry.csv header-only, synthetic_test_evidence/ preserved
matlab/data/          → 12 .m files (12 checked, parse-clean)
docs/                 → 9 markdowns + generated PHASE1_AUDIT_REPORT.md / VALIDATION_REPORT.md
```

Manifest loader correctly logs per `buildManifest.m:16`:
```
[buildManifest] Scanning APTOS2019 : ...\data\raw\APTOS2019 → DATASET NOT PRESENT — DOWNLOAD REQUIRED
[buildManifest] Scanning IDRiD ... → DATASET NOT PRESENT
[buildManifest] Scanning DRIVE ... → 0 masks
[buildManifest] Scanning Messidor2 ... → No metadata CSV/XLSX found — DO NOT assume labels exist
[buildManifest] No images found across all datasets.
```

No raw files were modified (`validatePhase1:rawNotModified PASS`).

---

## C. Manifest Status

- **Unified manifest**: `data/processed/manifest.csv` and `.mat` (header-only, 0 rows), generated by `matlab/data/generateManifest.m:15` → `buildManifest.m:1` with `computeHash false` (empty) / synthetic test used `computeHash true` and produced `manifest_synthetic_23rows.csv` (23 rows) preserved in `results/synthetic_test_evidence/`.
- **Columns (23, `docs/MANIFEST_SCHEMA.md`)**: `image_id, dataset, file_path, file_path_absolute, file_format, laterality, patient_id, subject_id, dr_grade, dr_grade_original, quality_status (=UNKNOWN), has_vessel_annotation, has_lesion_annotation, vessel_mask_path, lesion_annotation_path, split, width, height, channels, file_size_bytes, file_hash, provenance, status`. Missing → `UNKNOWN`/`NaN`/`''`, never invented.
- **Provenance**: `dataset ∈ {APTOS2019,IDRiD,DRIVE,Messidor2}`, `provenance` preserved, Messidor-2 auto `split=external`.
- **Empty production**: `height 0`, `byDataset all 0`. Synthetic proof: `APTOS 7, IDRiD 9, DRIVE 3, Messidor2 4` with hashes and dimensions verified.
- **Configurable paths**: `datasetConfig.m:15` governs all roots; `validatePhase1:configurablePaths PASS`.

---

## D. Annotation Registry Status

### IDRiD — `registerIDRiDAnnotations.m:1` → `results/idrid_annotation_registry.csv`

- **Empty production**: `0 rows, 0 fundus, 0 annotFiles` — `summary.status NOT PRESENT`, `has_annotation 0`, lesions all `NOT AVAILABLE` (honest).
- **Synthetic proof**: `7 rows` (MA 2, HE 2, EX 2, NONE 1), 6 annot files discovered, `IDRiD_Disease_Grading_Training_Labels.csv` parsed, orphan handling verified. The live run with empty raw correctly reports `0` without crashing.

Per-spec categories recorded as:

| Category | Empty | Synthetic (proof) |
|----------|-------|-------------------|
| MA (microaneurysm) | NOT AVAILABLE | AVAILABLE 2 |
| HE (hemorrhage) | NOT AVAILABLE | AVAILABLE 2 |
| EX (hard exudate) | NOT AVAILABLE | AVAILABLE 2 |
| SE (soft exudate) | NOT AVAILABLE | NOT AVAILABLE (not synthesized) |
| OD (optic disc) | NOT AVAILABLE | NOT AVAILABLE |
| Other anatomical | UNKNOWN | UNKNOWN |

### DRIVE — `registerDRIVEMasks.m:1` → `results/drive_vessel_registry.csv`

- **Empty production**: `0 rows, 0 fundus, 0 1st_manual, 0 mask` — `status NOT PRESENT`, image↔mask correspondence not testable (no data).
- **Synthetic proof**: `3 rows` (`training 2 + test 1`), `has_vessel_annotation 3`, `has_fov_mask 2`, `1st_manual 3`, numeric-ID mapping `21_training.tif ↔ 21_manual1.gif` verified, `dataset_split` inferred (training/test).
- **Live check**: correctly reports 0, does not fabricate masks.

Both registries re-run on every `register*` call; `validatePhase1:annotationMapping PASS` in both states.

---

## E. Dataset Audit Results

Run: `runAudit([], 'computeHash', true)` → `results/audit_results.json` + `docs/PHASE1_AUDIT_REPORT.md` (generated via `auditDataset.m:1`).

### Empty production (current)

- `totalImages 0`, `byDataset {APTOS2019:0, IDRiD:0, DRIVE:0, Messidor2:0}`
- `byGrade {L0:0 L1:0 L2:0 L3:0 L4:0 UNKNOWN:0}`, `byGradePerDataset` all zero
- `dimensions UNKNOWN`, `channels {}` , `byFormat {}`, `laterality {UNKNOWN:0}`
- `missingCount 0, unreadableCount 0, duplicateFilenameGroupCount 0, duplicateHashGroupCount 0`
- `annotationAvailability {hasVessel:0 hasLesion:0}`, per-dataset all zero
- `patientStats {totalRows:0 uniquePatientsKnown:0 unknownPatientRows:0 patientIdAvailable:false}`
- `status NOT VALIDATED — DATASET NOT PRESENT`, `summary Total=0 ... DATASET NOT PRESENT — DOWNLOAD REQUIRED`

Human tables in `PHASE1_AUDIT_REPORT.md` correctly show `NOT PRESENT` rows for all datasets and `0` class counts.

### Synthetic proof (preserved)

- `totalImages 23`, `APTOS2019 7 IDRiD 9 DRIVE 3 Messidor2 4`
- `byGrade L0 3 L1 1 L2 2 L3 2 L4 1 UNKNOWN 14` (labeled 9/23)
- `dimensions 64×64 min .. 80×64 max mean 70.5×64.0 uniqueSizes 2`, `channels C1 7 C3 15 UNKNOWN 1`, `byFormat JPG 3 PNG 7 TIF 13`, `laterality UNKNOWN 23`
- `missing 0 unreadable 1 (corrupt.png with imread failed, reported not crashing) duplicateHashGroup 1 (Messidor duplicate pair)` — duplicate detection via `computeFileHash.m:1` verified
- `hasVessel 3 hasLesion 8` per-dataset correct

**Verification**: `validatePhase1:countsConsistent PASS` (0==0 / 23==23).

---

## F. Leakage Results

Implemented: `generateSplits.m:checkLeakage()` + `validatePhase1:checkLeakage` + `checkSplitReproducibility`.

- **Empty**: `empty manifest — no leakage` (trivially PASS, documented limitation: no known `patient_id` to verify — `patientStats.patientIdAvailable false`). `externalLeakage false`.
- **Synthetic**: `No patient appears in multiple splits (6 patients checked)` — `patientLeakage false`, `externalLeakage false`, stored in `split_metadata_synthetic.json:leakageCheck`.

Production `data/splits/split_metadata.json` currently:
```json
{"seed":42,"ratios":{"train":0.7,"val":0.15,"test":0.15},"status":"NOT VALIDATED — EMPTY MANIFEST","leakagePolicy":"No patient in multiple splits; external isolated..."}
```

Messidor-2 isolation verified: all 4 synthetic Messidor rows stayed `external`; empty `external.csv` correctly header-only.

---

## G. Split Results

Config: `datasetConfig.m:splitRatios 70/15/15, seed 42, externalDatasets {Messidor2}`

- **Empty production**: `train 0, val 0, test 0, external 0, UNKNOWN 0` (header-only CSVs 290 B each), `split_metadata.json:status NOT VALIDATED — EMPTY MANIFEST`. Deterministic re-run yields identical header-only files (`validatePhase1:splitReproducible PASS`).
- **Synthetic proof**: `train 13, val 2, test 4, external 4` (19 dev pool), `strategy patient-grouped where available stratified-by-grade Messidor2 isolated`, `nPatientsGrouped 6 nSingletonImages 13`, `leakageCheck PASS` — preserved in `results/synthetic_test_evidence/`.

Development vs external separation per `docs/DATA_LEAKAGE_POLICY.md` and `docs/DATASET_PROVENANCE.md` maintained (DRIVE/APTOS/IDRiD pooled for dev, Messidor-2 never mixed).

---

## H. validatePhase1 Result

Run: `validatePhase1()` → `results/phase1_validation.json/.mat` + `docs/PHASE1_VALIDATION_REPORT.md`

### Empty production (current, honest)

```
Overall: PASS (10/10 checks passed, 100%)
| parse             | PASS | 12 files checked |
| configurablePaths | PASS | configurable=1 |
| manifestGeneration| PASS | rows=0 |
| unreadableHandling| PASS | corrupt file correctly reported; audit correctly counts missing/unreadable |
| countsConsistent  | PASS | manifest=0 audit=0 sumByDataset=0 |
| annotationMapping | PASS | IDRiD rows=0 DRIVE rows=0 (infrastructure validated) |
| splitReproducible | PASS | empty manifest — deterministic by definition |
| noLeakage         | PASS | empty manifest — no leakage |
| rawNotModified    | PASS | raw mtimes unchanged |
| resultsGenerated  | PASS | audit generated, total=0 |
```

Synthetic run also `PASS 10/10` (rows 23) preserved in `results/synthetic_test_evidence/phase1_validation_synthetic.json`.

**Gate**: `validatePhase1 = PASS` but `totalImages = 0` → gate **fails** per `§13` (all four datasets must be >0).

---

## I. Missing / Unknown Items (Honest)

| Item | Status | Note |
|------|--------|------|
| APTOS `train_images/*.png` | **MISSING** | No Kaggle download present; `train.csv` absent → `dr_grade NaN` for all APTOS rows (0 rows) |
| APTOS laterality/patient_id | **UNKNOWN** | Kaggle publishes no patient linkage; per `DATA_LEAKAGE_POLICY.md` will be `UNKNOWN` even when images present |
| IDRiD lesion GT (MA/HE/EX/SE/OD) | **NOT AVAILABLE** (0 rows) | Expected under `A. Segmentation/2. All Segmentation Groundtruths/`; scanner found 0 |
| IDRiD grading CSV | **NOT AVAILABLE** | `B. Disease Grading/2. Groundtruths/*.csv` not found |
| DRIVE masks (`1st_manual/mask`) | **NOT AVAILABLE** | `training/1st_manual/*.gif` not found |
| Messidor-2 images | **MISSING** | ADCIS download not present; expected 1748 `*.tif` |
| Messidor-2 DR labels | **UNKNOWN** | Official source provides no bundled CSV; `parseMessidor2Labels` will leave `dr_grade NaN` and report exact sentence `"External image dataset available, but DR ground-truth availability must be verified before sensitivity/specificity evaluation."` |
| `quality_status` | **UNKNOWN** for all rows (Phase 1 boundary) | No Phase 2 quality module yet — `matlab/quality/` remains placeholder `README.md` |
| Patient IDs for leakage | **UNKNOWN** for DRIVE/Messidor-2 | Fallback to image-level stratified split documented |

None are fabricated; all recorded as `UNKNOWN`/`NaN`/`NOT PRESENT` where appropriate.

---

## J. Exact Next Action (Manual Download Required)

> **STOP after Phase 1.5 verification — do NOT start Phase 2.**  
> Gate currently: `totalImages 0 → FAIL`. **DATASET NOT DOWNLOADED — MANUAL DOWNLOAD REQUIRED** for all four.

### 1. Download (official sources only)

**APTOS** https://www.kaggle.com/c/aptos2019-blindness-detection — `Kaggle → Join Competition → aptos2019-blindness-detection.zip → extract to `data/raw/APTOS2019/` (preserve `train_images/`, `train.csv`)**

**IDRiD** https://ieeedataport.org/open-access/indian-diabetic-retinopathy-image-dataset-idrid — `IEEE DataPort → Add to Cart → Checkout → IDRiD_dataset.zip → extract to `data/raw/IDRiD/` (preserve `A. Segmentation/` + `B. Disease Grading/`)**

**DRIVE** https://drive.grand-challenge.org/ — `Grand Challenge → Download → DRIVE.zip → extract to `data/raw/DRIVE/` (preserve `training/{images,1st_manual,mask}` + `test/...`)**

**Messidor-2** https://www.adcis.net/en/third-party/messidor2/ — `ADCIS form → email link → Messidor-2.zip → extract to `data/raw/Messidor2/` + place optional grades spreadsheet alongside images**

Do not use unofficial mirrors. Preserve `.gitkeep` alongside but do not commit images (`.gitignore` already excludes `data/raw/`).

### 2. Re-verify (exact commands, from `C:\dev\SIH\DR_Screening` in MATLAB)

```matlab
addpath(genpath('matlab'));
cfg = datasetConfig();  % check cfg.projectRoot = C:\dev\SIH\DR_Screening

% Should now show >0 files per dataset:
numel(findImageFiles(cfg.aptosRoot))
numel(findImageFiles(cfg.idridRoot))
numel(findImageFiles(cfg.driveRoot))
numel(findImageFiles(cfg.messidor2Root))

generateManifest([], 'computeHash', true);      % → data/processed/manifest.csv should be >0 rows
runAudit([], 'computeHash', true);             % → results/audit_results.json + docs/PHASE1_AUDIT_REPORT.md
registerIDRiDAnnotations();                    % → results/idrid_annotation_registry.csv
registerDRIVEMasks();                          % → results/drive_vessel_registry.csv
generateSplits();                              % → data/splits/{train,val,test,external}.csv + split_metadata.json (seed 42)
validatePhase1();                              % → results/phase1_validation.json must be PASS
```

### 3. Gate check (must all be true)

```
APTOS2019 totalImages > 0
AND IDRiD totalImages > 0
AND DRIVE totalImages > 0
AND Messidor2 totalImages > 0
AND validatePhase1 = PASS
AND byGradePerDataset.Messidor2 split=external
AND patient leakage = false
```

Inspect `docs/PHASE1_AUDIT_REPORT.md` — both tables must show `>0` images and class distribution; `results/audit_results.json:byDataset` confirms.

### 4. Only then

If gate passes, report:

> **"REAL DATASET GATE PASSED — READY FOR PHASE 2 PROMPT."**

Do not implement `matlab/quality/` (focus/illumination/FOV/glare/vignetting/contrast → `GOOD/BORDERLINE/UNGRADABLE`) until that gate is documented. Phase 1/1.5 remains locked (`quality_status UNKNOWN`).

---

## STOP Condition

- Current: `totalImages 0` → **REAL DATASET GATE FAILED — MANUAL DOWNLOAD REQUIRED** → stop, await downloads.
- Synthetic `23 rows PASS 10/10` evidence preserved separately proves pipeline works when data present, but does not satisfy the real-data gate.

No clinical claims made. No Phase 2 code created.

